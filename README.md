Display ncurses color palette with representative numbers for each color or display custom color using rgb values.

Usage:
```bash
gcc -lncurses ncolors.c -o ncolors
```

```bash
./ncolors --help
```
